import { View, Text } from "react-native";

export default function Task() {
    return (
        <View
            style={{
                flex: 1,
                justifyContent: "center",
                alignItems: "center",
            }}
        >
            <Text>Task</Text>
        </View>
    );
}